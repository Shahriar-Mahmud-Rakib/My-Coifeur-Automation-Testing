[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = --tb=long --no-header --json-report-indent=2
timeout = 60
markers =
    system_selftest: validates the QA system's own detector logic (not the app under test). Filtered from user-facing report counts.
